# Screenshots

Two images belong here for the L1 submission:

| File | Shows |
|---|---|
| `compile.png` | `compact compile` finishing successfully, with the circuits listed |
| `deploy.png` | the deploy finishing, with the contract address visible |

Both are captured after the toolchain is in place — see
[../SETUP-WINDOWS.md](../SETUP-WINDOWS.md). Once they exist, link them from the
main README next to the contract address table.
